# Comind-Ops Platform - External Services Configuration
# Copy this file to .env and customize the values

# Environment
ENV=dev

# PostgreSQL Configuration
POSTGRES_PASSWORD=secure_password_change_me
POSTGRES_DB=comind_ops
POSTGRES_USER=comind_ops_user

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Backup Configuration
BACKUP_RETENTION_DAYS=7

# Network Configuration
# These are automatically configured in docker-compose.yml
# POSTGRES_HOST=172.20.0.10
# MINIO_HOST=172.20.0.20
